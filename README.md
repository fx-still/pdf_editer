# PDF 极简工具站

只做两件事：**合并 PDF**、**拆分 PDF**（Range / Every N / Single pages）。

## 特性

- 纯前端本地处理（不上传服务器）
- 拖拽上传、移动端可用
- 合并支持拖拽排序
- 拆分支持范围解析与校验，输出 zip
- PWA（生产环境启用；开发环境默认关闭）

## 本地启动

```bash
npm install
npm run dev
```

打开 `http://localhost:3000`。

